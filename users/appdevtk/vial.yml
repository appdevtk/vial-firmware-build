---
- fork: # optional
    username: appdevtk # required
    repository: vial-qmk # optional, defaults to vial-qmk
    branch: my-config # optional, defaults to vial
  keyboard: splitkb/aurora/lily58 # required
  keymaps: [vial] # required